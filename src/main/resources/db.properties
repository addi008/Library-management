# Database Configuration

# Choice 1: Standard MySQL Server Setup
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/library_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=root

# Choice 2: Embedded H2 database mode (MySQL compatibility mode for zero-setup execution)
# db.driver=org.h2.Driver
# db.url=jdbc:h2:mem:library_db;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;INIT=RUNSCRIPT FROM 'schema.sql'
# db.user=sa
# db.password=
